SCENARIO 1: Concurrent StartPomodoro (5 simultaneous POSTs → exactly 1 succeeds)
================================================================================
Date: 2026-05-07T01:04 UTC
Server: localhost:8081
Room: qa-test-room | User: qatester (persistent, is_owner)

PRECONDITIONS:
- Clean test database
- No active pomodoro session
- Token obtained via CreateRoom

EXECUTION:
  5 simultaneous POST /api/pomodoro/start sent via backgrounded curl:

  curl -X POST http://localhost:8081/api/pomodoro/start \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"room_name":"qa-test-room","planned_duration":1500}'

RESULTS (per request):
  Req 1: HTTP 201 - SESSION CREATED (success, session_id returned)
  Req 2: HTTP 400 - REJECTED (error: "already_following")
  Req 3: HTTP 400 - REJECTED (error: "already_following")
  Req 4: HTTP 400 - REJECTED (error: "already_following")
  Req 5: HTTP 400 - REJECTED (error: "already_following")

  Successes: 1 | Failures: 4 | Exactly as expected

DB VERIFICATION:
  SELECT COUNT(*) FROM pomodoro_sessions
  WHERE member_id='6ee09523-3de8-4bf5-bd61-db61715db879' AND ended_at IS NULL;
  → 1 active session (correct)

ANALYSIS:
  The RunInTx-wrapped StartPomodoro correctly prevents duplicate active sessions.
  The transaction serializes concurrent requests — only the first one inserts,
  subsequent ones detect the existing active session and return error.

VERDICT: PASS ✓
